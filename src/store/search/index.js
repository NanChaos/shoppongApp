//search的store

// state:仓库存储数据
const state = {
    mack: 2
};
// mutations:修改仓库的唯一手段
const mutations = {};
// action:处理action，可以书写1自己的业务逻辑，也可以处理异步
const actions = {};
// getters：计算属性
const getters = {};

// 暴露
export default {
    state,
    mutations,
    actions,
    getters
}